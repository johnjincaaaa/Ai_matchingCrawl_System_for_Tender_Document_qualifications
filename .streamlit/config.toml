# Streamlit配置文件

# 设置应用的基本配置
[server]
# 服务器端口
port = 8501
# 允许来自任何IP的连接
enableCORS = false
# 禁用XSRF保护以兼容CORS配置
enableXsrfProtection = false

# 设置日志配置
[logger]
# 设置日志级别（debug, info, warning, error, critical）
level = "error"
