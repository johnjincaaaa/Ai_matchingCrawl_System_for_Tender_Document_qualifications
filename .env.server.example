# ============================================================
# 服务器部署配置模板
# 用法：在【服务器】上把本文件复制为 .env（同目录），即可生效。
#   Windows PowerShell:  Copy-Item .env.server.example .env
#   Linux/macOS:         cp .env.server.example .env
# .env 本身不进 git（各机器各配各的），本模板会随 git 同步。
# 本地开发机已有自己的 .env（指向本机局域网地址），无需改动。
# ============================================================

# 报告「下载链接」的 HTTP 根地址：指向服务器上运行的 Streamlit
# （已内置 /tender-files/ 静态路由，把 tender_files 目录暴露在该端口）。
# 报告里的下载链接会拼成 http://114.215.171.34:8501/tender-files/<平台>/<文件名>。
# 结尾不要带斜杠。换公网IP/端口/域名时改这里即可。
APP_PUBLIC_BASE_URL=http://114.215.171.34:8501

# tender_files 对外访问前缀（与 Streamlit 静态路由一致，一般保持默认）
TENDER_FILES_URL_PREFIX=/tender-files

# ------------------------------------------------------------
# 部署前提（务必确认）：
# 1. 服务器防火墙/安全组放行 8501 端口，否则外网访问不到（能打开 Streamlit 界面即说明已通）。
# 2. 导出报告时 Streamlit 需保持运行（文件下载服务挂在 Streamlit 上）。
# 3. 安全提醒：/tender-files/ 无鉴权，任何知道链接的人都能下载服务器上全部标书文件。
#    若标书内容敏感，建议在 nginx 等网关层加 Basic Auth 或限制来源 IP。
# ------------------------------------------------------------
